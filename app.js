var numero = parseFloat(prompt("Ingresa un número"));
if (numero>0) {
        document.write("Este es un número positivo");    
    }
else {
        document.write("Este es un número negativo");
    }
	
